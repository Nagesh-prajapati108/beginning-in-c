#include <stdio.h>
int main() {
    char s1[100], s2[100];
    int i;

    printf("Enter string s1: ");
    fgets(s1, 100, stdin);

    for (i = 0; s1[i] != '\0'; i++) {
        s2[i] = s1[i];
    }

    s2[i] = '\0';
    printf("Copied first string: %s", s2);

    
    return 0;
}
