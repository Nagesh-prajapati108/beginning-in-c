#include<stdio.h>
#include<string.h>

int main()
{
    int i,j;
    char s1[100],s2[100];

    printf("enter the string: ");
    fgets(s1,100,stdin);
    
    s1[strcspn(s1, "\n")] = '\0';

    i=0;
    j=strlen(s1)-1;

    while(j>=0){
        s2[i]=s1[j];
        i++;
        j--;
    }
        s2[i]='\0';

    if(strcmp(s1,s2)==0)
    printf("palindrom");

    else
    printf("not");



    return  0;
}


//fgets ends with a "\n"(new line) so while comparing it generates error.
//strcspn(s1,"\n") find postion of "\n" and is used to replace by"\0".
