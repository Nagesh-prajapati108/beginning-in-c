#include<stdio.h>
int sum(int n){
    if(n==0)
    return 0;
    
    else
    return n+sum(n-1);
}
int main()
{
    int n ;
    printf("enter the number of terms: ");
    scanf("%d",&n);

    int result=sum(n);
    
    printf("sum upto n terms: %d",result);
    
    return 0;
}
